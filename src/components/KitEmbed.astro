---
/** Kit modal form — load once per page (Grow → Forms → Embed → JavaScript) */
import { KIT_FORM_UID, kitEmbedSrc } from '../config';
---

<script is:inline async data-uid={KIT_FORM_UID} src={kitEmbedSrc}></script>
